# Frontend: optional. If omitted, DevCity reads public repositories directly from api.github.com.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Backend: optional but recommended to increase GitHub API rate limits.
GITHUB_TOKEN=
